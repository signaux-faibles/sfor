<%= turbo_stream.replace "flash" do %>
  <div id="flash" class="fr-alert fr-alert--success">
    <%= notice %>
  </div>
<% end %>

<%= turbo_stream.replace @summary.is_codefi ? "codefi_summary" : "segment_summary" do %>
  <%= render partial: "summaries/summary", locals: { summary: @summary, establishment: @establishment, establishment_tracking: @establishment_tracking } %>
<% end %>