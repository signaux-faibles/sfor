<%# Link to the "First" page
  - available local variables
    url:           url to the first page
    current_page:  a page object for the currently displayed page
    total_pages:   total number of pages
    per_page:      number of items to fetch per page
    remote:        data-remote
-%>
<% pagination_link_data = local_assigns[:turbo] == false ? { turbo: false } : {} %>
<%= if current_page.first?
      content_tag(:a, t('views.pagination.first').html_safe, class: 'fr-pagination__link fr-pagination__link--first', aria: { disabled: 'true' }, role: 'link')
    else
      link_to(t('views.pagination.first').html_safe, url, remote: remote, class: 'fr-pagination__link fr-pagination__link--first', aria: { disabled: 'false' }, role: 'link', data: pagination_link_data)
    end %>
