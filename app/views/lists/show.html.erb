<% content_for :title, "Liste #{@list.label}" %>

<div class="fr-container">
  <nav role="navigation" class="fr-breadcrumb" aria-label="vous êtes ici :">
    <button class="fr-breadcrumb__button" aria-expanded="false" aria-controls="breadcrumb">
      Voir le fil d'Ariane
    </button>
    <div class="fr-collapse" id="breadcrumb">
      <ol class="fr-breadcrumb__list">
        <li>
          <%= link_to 'Accueil', '/', class: 'fr-breadcrumb__link' %>
        </li>
        <li>
          <%= link_to 'Listes de détection', '/listes', class: 'fr-breadcrumb__link' %>
        </li>
        <li>
          <a class="fr-breadcrumb__link" aria-current="page">Liste <%= @list.label %></a>
        </li>
      </ol>
    </div>
  </nav>
</div>

<div class="fr-container fr-mt-5w fr-mb-5w">
  <div class="grid-row">
    <h1>Liste <%= @list.label %></h1>

    <%= render 'search_form', search_params: @search_params || {}, list: @list %>

    <h2 class="fr-mt-5w">Résultats de recherche</h2>

    <% if @results && @results.any? %>
      <div class="fr-grid-row fr-grid-row--gutters fr-mb-2w">
        <div class="fr-col-12">
          <%= link_to list_path(@list, search: @search_params, format: :xlsx),
                      class: 'fr-link--download fr-link',
                      data: { controller: 'download', action: 'click->download#disable', download_target: 'button' },
                      id: 'download-button' do %>
            Télécharger les <%= @results.size %> résultats de la recherche
            <span class="fr-link__detail">XLS</span>
          <% end %>
        </div>
      </div>
      <p class="fr-mt-2w">
        <%= pluralize(@results.size, "entreprise", "entreprises") %>
        <%= turbo_frame_tag "alert_breakdown", 
            src: alert_breakdown_list_path(@list, search: @search_params),
            loading: "lazy" do %>
          <!-- Loading alert breakdown... -->
        <% end %>
      </p>

      <div class="fr-mt-3w">
        <div class="fr-grid-row fr-grid-row--gutters">
          <% @results.each do |company| %>
            <div class="fr-col-12">
              <div class="fr-card fr-card--sm">
                <div class="fr-card__body">
                  <div class="fr-card__content">
                    <h3 class="fr-card__title">
                      <%= link_to company["nom_complet"], company_path(company["siren"]) %>
                    </h3>
                    <p class="fr-card__desc">
                      SIREN : <%= company["siren"] %>
                    </p>
                    <div class="fr-card__start">
                      <%= turbo_frame_tag "company_badges_#{company["siren"]}", 
                          src: enrich_company_list_path(@list, siren: company["siren"]),
                          loading: "lazy" do %>
                        <ul class="fr-badges-group">
                          <!-- Loading badges... -->
                        </ul>
                      <% end %>
                    </div>
                    <div class="fr-card__end">
                      <%= turbo_frame_tag "company_establishments_#{company["siren"]}", 
                          src: enrich_company_list_path(@list, siren: company["siren"]),
                          loading: "lazy" do %>
                        <!-- Loading establishment count... -->
                      <% end %>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          <% end %>
        </div>
      </div>

    <% else %>
      <div class="fr-alert fr-alert--info fr-mt-3w">
        <p>Aucun résultat trouvé.</p>
      </div>
    <% end %>
  </div>
</div>

